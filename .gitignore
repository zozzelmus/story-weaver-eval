# The live profile is created by /learn-team. Keep the template tracked; the filled-in
# profile is yours to commit or ignore. Commit it if you want the learned conventions
# versioned and reviewable (recommended). To keep it local instead, uncomment:
# profile/team-profile.md

# OS / editor noise
.DS_Store
*.swp
